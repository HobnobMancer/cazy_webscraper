# HTML pages for unit tests

These files are for testing the function `get_cazy_classes` within `scraper.crawler.__init__.py`, which retrieves the URLs for CAZy class summary pages from the CAZy homepage.